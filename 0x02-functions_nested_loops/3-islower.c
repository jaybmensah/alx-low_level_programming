#include "main.h"

/**
 * main - code that checks for lowercase character.
 *
 * Return: Always 0.
 */

int _islower(int c)
{
	int i;

	if (i >= 97 && i <= 122)
	{
		return (1);
	}

	else

return (0);
}
