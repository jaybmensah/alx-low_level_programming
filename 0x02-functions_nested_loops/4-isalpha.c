#include "main.h"

/**
 * main - code that checks for lowercase character.
 *
 * Return: Always 0.
 */

int _isalpha(int c)
{
	int i;

	if (i >= 65 && i <= 90)
	{
		return (1);
	}

	else

return (0);
}
