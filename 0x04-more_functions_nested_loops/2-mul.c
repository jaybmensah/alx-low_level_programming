#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: is a digit placeholder
 * @b: is a digit placeholder
 * Return: Return c.
 */
int mul(int a, int b)
{
	int a, b, c;

	c = mul(a, b);

	return (c);
}
